services:
  phpmyadmin:
    image: chrisnharvey/magiclamp-phpmyadmin:${MAGICLAMP_VERSION:-1.6}

    networks:
      app_net:
        ipv4_address: 10.0.10.22

    environment:
      - PMA_HOST=10.0.10.20
      - PMA_USER=root
      - PMA_PASSWORD=
      - MYSQL_ROOT_PASSWORD=
